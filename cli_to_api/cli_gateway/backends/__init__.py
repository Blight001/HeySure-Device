"""Platform-specific gateway implementations.

Only CLI invocation, response parsing and provider-specific authentication
belong here. Shared protocol/session helpers live in ``cli_gateway.shared``.
"""
