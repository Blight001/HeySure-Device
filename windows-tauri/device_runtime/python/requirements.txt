# HeySure 桌面端 Python 运行时依赖（Windows）。
# 与 device/windows/device_runtime/python/requirements.txt 保持一致。

pyautogui>=0.9.54
pynput>=1.7.6
pyperclip>=1.8.2
mss>=9.0.1
Pillow>=10.0.0
psutil>=5.9.0
pygetwindow>=0.0.9
pywinauto>=0.6.8
uiautomation>=2.0.18
pyttsx3>=2.90

# 视觉 / OCR（较重，按需取消注释）：
# opencv-python>=4.9.0
